# <id> — <Title>

## Problem

## Target state

Present tense, the durable core. Observable behavior first — contracts, outcomes, what a caller can see — then only the internal decisions that constrain the work: public interfaces, data models, patterns to follow. Interior implementation stays open.

## Non-goals

## Open questions

Every line resolved before implementation starts: `- [resolved] <question>? → <answer>`

## Acceptance criteria
