<!-- Appended verbatim to the consuming repo's AGENTS.md (or CLAUDE.md — whichever it uses, never
both) by the setup skill. Replace a block from a prior run rather than appending a duplicate. -->

## Agentic coding workflow

This repository uses the agentic coding workflow. Read
`${CLAUDE_PLUGIN_ROOT}/workflow/lifecycle.md` before starting work in any stage — it owns the
stages, the human gates, and what dispatches what. Two files in this repository belong to it:
`${CLAUDE_PROJECT_DIR}/work/config.conf` (settings the scripts read) and
`${CLAUDE_PROJECT_DIR}/docs/conventions/git.md` (conventions a human follows).
